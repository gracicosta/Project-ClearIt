# Notas & Próximos Passos — Definição do Levels e Bases de Conhecimento

## Notas e Aprendizados
- A definição do Framework de Levels agora nos dá segurança total para modelar a tabela `PdiObjective` no Prisma ORM (SQLite).
- As restrições de CSS do React-PDF foram expostas claramente, prevenindo erros clássicos de layout no frontend.

## Próximos Passos
- Avançar para o **Passo 1 do Bootstrap** do projeto físico: inicializar monorepo, criar package.json raiz com `concurrently` e preparar os subdiretórios `backend/` e `frontend/`.
